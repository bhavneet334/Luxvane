<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit product page</title>
    <link rel="stylesheet" href="/stylesheets/dashboard.css" />
    <link rel="stylesheet" href="/stylesheets/edit-product.css">
    <link rel="stylesheet" href="/stylesheets/header.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>

     <div class="product-container">
      <main class="product-form-page">

        <% if (success && success.length > 0) { %>
            <div class="flash flash-success"><%= success %></div>
        <% } %>

        <% if (error && error.length > 0) { %>
            <div class="flash flash-error"><%= error %></div>
        <% } %>

        <div class="product-form-header">
          <h1>Edit Product</h1>
          <a href="/owners/products" class="btn btn-outline"
            >Back to Products</a
          >
        </div>


        <form actions="/owners/products/<%=product._id%>/update" method="POST" class="product-form">

          <div class="form-row">
            <label for="name">Name</label>
            <input
              id="name"
              name="name"
              type="text"
              placeholder="Product name"
              required
            />
          </div>

          <div class="form-row">
            <label for="price">Price</label>
            <input
              id="price"
              name="price"
              type="number"
              step="0.01"
              placeholder="e.g. 49.99"
              required
            />
          </div>

          <div class="form-row">
            <label for="image">Image URL</label>
            <input
              id="image"
              name="image"
              type="text"
              placeholder="https://..."
              required
            />
          </div>

          <div class="form-row">
            <label for="description">Description</label>
            <textarea
              id="description"
              name="description"
              rows="3"
              type="text"
              placeholder="Description"
            ></textarea>
          </div>

          <div class="form-row">
            <label for="discount">Discount (%)</label>
            <input
              id="discount"
              name="discount"
              type="number"
              min="0"
              placeholder="Optional"
            />
          </div>

          <div class="form-actions">
            <button type="submit" class="btn btn-primary">
              Update Product
            </button>
            <a href="/owners/products" class="btn btn-outline">Cancel</a>
          </div>
        </form>
      </main>
    </div>
  </body>
</html>

    
</body>
</html>